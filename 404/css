category(
id int primary auto_incrememnt
category_name,
order_by,
create_at ,
update_at
)

course (
id int primary autoincrement
coursename ,
tags,
order,
category_id
createat,
updatea_at
)

quiz(
id int primary autoincrement

)

practice(
id int primary autoincrement
) // questions and given answer

module(
id int primary autoincrement
)

content(
id int primary autoincrement
)

modeul_quiz(
id int primary autoincrement
)

